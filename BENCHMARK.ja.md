[English](BENCHMARK.md) | [한국어](BENCHMARK.ko.md) | **日本語** | [中文](BENCHMARK.zh.md) | [Español](BENCHMARK.es.md)

# caching.ai は本当にコストを削減できるのか？実測しました。

3 アーム、6 種のトラフィックパターン、7 モデル、**定価で 10,000 件以上の実 API コール**（2026-07、proxy v0.10.0）。ここに掲載したすべての内容は、ご自身のキーで再現可能です——手法、フィクスチャ、ランナー、生ログは [`bench/`](bench/README.ja.md) にあります。

**アーム。** A = プロバイダーを直接呼び出し、キャッシュヒントなし（SDK デフォルト）。B = 直接呼び出しで、プロキシが配置するのとまったく同じ位置に `cache_control` を手動配置（Anthropic のみ — OpenAI/Gemini/Grok は自動キャッシュのため、そこでは A ≡ B）。C = 同じリクエストを caching.ai 経由で送信し、**キープアライブ ping コストを差し引いた純額**。コストはプロバイダー報告の使用トークン数 × 公開定価。会話スクリプトは固定。アームごとのソルトトークンにより、アーム間でプロバイダーキャッシュを共有することは不可能です。詳細：[`bench/README.ja.md`](bench/README.ja.md)。

<picture>
  <source media="(prefers-color-scheme: dark)" srcset=".github/assets/bench-scenarios-dark.svg">
  <img alt="Input-side cost of arms A/B/C across scenarios on claude-haiku-4.5" src=".github/assets/bench-scenarios-light.svg" width="820">
</picture>

## caching.ai が勝つ場面

| ワークロード（claude-haiku-4.5） | A 直接 | C caching.ai（純額） | 削減率 |
|---|---|---|---|
| S2 sparse support — 12 コール、6–9 分アイドル | $0.0720 | **$0.0240**（16 ping 込み） | **67%** |
| S1 agent loop — 40 コール、0–90 秒間隔 | $0.4104 | **$0.1378** | **66%** |
| S4 classify batch — 300 コール | $1.7268 | **$0.1868** | **89%** |
| S6 steady traffic — 60 コール、30 秒間隔 | $0.3123 | **$0.0387** | **88%** |

claude-sonnet-5 でも同じパターン：直接呼び出し比で S2 **68%**、S1 **69%** 削減。

これを支える要因は 2 つあります。

1. **Anthropic のキャッシュはオプトインで、ほとんどの統合はオプトインしていません。** アーム A のヒット率は、すべての Anthropic セルで 0% です——これが SDK デフォルトのトラフィックの実態です。C はブレークポイントを自動注入し、手動チューニングした B とトークン単位で一致します（S1/S4/S6：B と C はバイト単位で同一）。
2. **短い TTL はアイドル間隔で死にます。** S2 では、手動チューニングした B は素朴な A より実際に **25% 高く**つきます。B のキャッシュは 6–9 分の間隔のたびに期限切れになり、1.25 倍の書き込みプレミアムを 12 回支払いながら、読み取りはゼロだからです。C のキープアライブはプレフィックスを温かく保ち（ヒット率 91%）、すべての ping コストを支払ってもなお 67% の削減を達成します。

## GPT-5.6：新モデルが失ったキャッシュを、プロキシが復元します

GPT-5.6 世代のモデルは**ブレークポイントスコープのキャッシュ**に移行し、暗黙のブレークポイントは*最新のメッセージ*に置かれます——そのため、共有システムプロンプトを使う素の SDK トラフィックでは、リクエスト間プレフィックスヒットが **0%** になります（`gpt-5.6-sol` で数千件のコールを実測：ヒットしたのはバイト単位で同一の繰り返しプロンプトのみ）。プロキシは、リクエスト自体がキャッシュパラメータを持たない場合に、文書化された対処法——共有プレフィックスの末尾への明示的な `prompt_cache_breakpoint` と安定した `prompt_cache_key`——を chat/completions と Responses API の両方に注入します。実測結果（`gpt-5.6-sol`、上記と同じハーネスとアーム）：

| ワークロード | A 直接（ヒット率） | C caching.ai（ヒット率） | 削減率 |
|---|---|---|---|
| S6 steady traffic — 60 コール、30 秒間隔 | $1.6964 (0%) | **$0.1705 (97.8%)** | **90%** |
| S2 sparse support — 12 コール、6–9 分アイドル | $0.3911 (0%) | **$0.0636 (91.0%)** | **84%** |

<picture>
  <source media="(prefers-color-scheme: dark)" srcset=".github/assets/bench-s2-models-dark.svg">
  <img alt="S2 sparse support: cost of each arm relative to direct, per model" src=".github/assets/bench-s2-models-light.svg" width="820">
</picture>

## モデルプロバイダーの挙動に関するメモ

- **OpenAI（5.6 以前）、Gemini、Grok**：これらのプロバイダーはアップストリーム側で自前でキャッシュを保持するため、プロキシはトラフィックをそのまま通過させます——ping なし、ルーティングキーの注入なし、書き込みプレミアムなし。S2 sparse での実測：gpt-4o は **$0.0878 vs $0.0878**（同一、双方 87.7% ヒット）、gemini-2.5-flash は **$0.0190 vs $0.0190** — バイト単位で同一のパススルーコスト。（当社の測定では gpt-5.5 はプロキシ経由で 23% 安くなりました——プロバイダー側のキャッシュルーティングのばらつきによるもので、通常は同等になると考えてください。）その上で、メータリング、ブレーカー診断、予算管理が利用できます。
- **不安定なプレフィックス**（システムプロンプト内のタイムスタンプやランダム ID）は、誰にもキャッシュできません。プロキシはダッシュボード上でブレーカーとその推定根本原因を特定し、プレフィックスが変化し続けている間は自身の注入を自動的に一時停止します——壊れたプロンプトが書き込みプレミアムを買うことは決してありません。
- **ウォームホールド**（チャットで "keep my cache warm for 2 hours"）：長時間のホールドは 1 回の 1 h-TTL キャッシュ書き込みと 1 時間ごとのリフレッシュで処理し、短時間のホールドは 0.1 倍の ping でつなぎます——指定されたウィンドウに対して安い方が選ばれます。

**レイテンシ。** プロキシのホップは、ほとんどのセルでプロバイダー側のノイズより小さい範囲に収まりました：TTFT p50 の差分は −77 ms（C が高速、キャッシュヒット）から +121 ms（純粋なパススルーセル）の範囲でした。

## 再現方法

```sh
node bench/setup-keys.mjs
node bench/orchestrate.mjs --run-id run-$(date +%Y%m%d) --budget 150
node bench/analyze.mjs --run-id run-...
```

公開したすべてのセルの生 JSONL（シークレットは書き込み時にリダクト済み、失敗も含む）は [`bench/results/run-202607-v0100/`](bench/results/run-202607-v0100/) にあり、セルごとの集計はその `summary.md` にあります。価格：2026-07 時点の公開定価（`bench/lib/pricing.mjs` 参照）。注意点：単一リージョン（クライアントはソウル）。Anthropic のセルは独立した 3 回の実行の平均、GPT-5.6 世代とパススルーのセルは proxy v0.10.0 上でセルあたり 1 回の実行で測定しました。
